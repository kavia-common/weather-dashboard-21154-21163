# Development-only server settings for CRA (Create React App)
# Allows binding to all interfaces in preview environments and disables host checking to fix
# "Invalid Host header" when accessed via tunneled/preview domains.
# NOTE: This has no effect on production builds.
HOST=0.0.0.0
DANGEROUSLY_DISABLE_HOST_CHECK=true
