# OpenWeather configuration (frontend)
# Copy this to .env and fill in your values. Do not commit real keys.

REACT_APP_OPENWEATHER_API_KEY=your_openweather_api_key

# Optional: some accounts must use One Call 3.0 instead of 2.5
# REACT_APP_OPENWEATHER_USE_ONECALL3=true
