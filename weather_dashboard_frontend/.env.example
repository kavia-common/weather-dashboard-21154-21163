# Environment variables for Weather Dashboard (React)
# Copy this file to ".env" in the same directory and fill in values as needed.

# PUBLIC_INTERFACE
# REACT_APP_OPENWEATHER_API_KEY
# Your OpenWeatherMap API key. If not set, the app uses the keyless fallback (Open‑Meteo + Nominatim).
REACT_APP_OPENWEATHER_API_KEY=your_openweather_api_key_here

# PUBLIC_INTERFACE
# REACT_APP_OPENWEATHER_USE_ONECALL3
# Some OpenWeather accounts require using One Call v3.0 to avoid 401/403.
# Set to "true" to force the app to call v3.0 for One Call requests.
# Leave unset or "false" to default to v2.5 (the app will automatically retry v3.0 on 401).
# Example:
# REACT_APP_OPENWEATHER_USE_ONECALL3=true

# IMPORTANT:
# - Do NOT use REACT_APP_REACT_APP_OPENWEATHER_API_KEY (this is a misnamed env var and will be ignored).
# - Respect OpenWeather rate limits; the fallback provider is used automatically when the key is missing.
